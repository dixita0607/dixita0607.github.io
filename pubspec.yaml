name: jaspr_site
description: A new Jaspr project
version: 0.0.1

environment:
  sdk: '>=3.10.0'

dependencies:
  jaspr: ^0.22.0
  jaspr_content: ^0.4.4

dev_dependencies:
  build_runner: ^2.6.0
  build_web_compilers: ^4.4.6
  jaspr_builder: ^0.22.0
  jaspr_lints: ^0.6.0
  lints: ^6.0.0

jaspr:
  mode: static

